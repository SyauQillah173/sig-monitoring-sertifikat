<div class="ui-settings-heading">
    <p class="ui-section-label">{{ __('Pengaturan Akun') }}</p>
    <h1 class="ui-settings-title">{{ __('Settings') }}</h1>
    <p class="ui-settings-subtitle">{{ __('Manage your profile and account settings') }}</p>
</div>
