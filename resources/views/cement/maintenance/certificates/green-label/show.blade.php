<x-layouts::app :title="'Detail Sertifikat Green Label'"><div class="ui-page"><x-ui.page-header eyebrow="Pemeliharaan Data" title="Detail Sertifikat Green Label" description="{{ $certificate->merekSemen?->nama_merek }} - {{ $certificate->sni }}"><x-slot:actions><a href="{{ route('cement.certificates.document', ['type' => 'green-label', 'certificate' => $certificate]) }}" class="ui-button-secondary">Download Dokumen</a><a href="{{ route('cement.maintenance.sertifikat-green-label.edit', $certificate) }}" class="ui-button-primary">Edit</a></x-slot:actions></x-ui.page-header>@include('admin.master-data.partials.flash-messages')<div class="ui-table-shell"><div class="grid gap-4 p-6 md:grid-cols-2"><p><strong>Merek:</strong> {{ $certificate->merekSemen?->nama_merek }} <span class="ui-table-row-meta">ID #{{ $certificate->merek_semen_id }}</span></p><p><strong>Kategori:</strong> {{ $certificate->merekSemen?->kategoriSemen?->nama_kategori }}</p><p><strong>SNI:</strong> {{ $certificate->sni }} <span class="ui-table-row-meta">ID master #{{ $certificate->sni_reference_id ?: '-' }}</span></p><p><strong>Komoditi:</strong> {{ $certificate->komoditi }} <span class="ui-table-row-meta">ID master #{{ $certificate->komoditi_reference_id ?: '-' }}</span></p><p><strong>Peringkat:</strong> {{ $certificate->peringkat }} <span class="ui-table-row-meta">ID master #{{ $certificate->peringkat_green_label_reference_id ?: '-' }}</span></p><p><strong>Lokasi:</strong> {{ $certificate->lokasi }} <span class="ui-table-row-meta">ID master #{{ $certificate->lokasi_pabrik_id ?: '-' }}</span></p><p><strong>Berlaku s.d:</strong> {{ $certificate->berlaku_sd->format('d M Y') }}</p><p><strong>Status:</strong> <span class="{{ $certificate->statusBadgeClasses() }}">{{ $certificate->statusLabel() }}</span></p><p><strong>File Asli:</strong> @if($certificate->certificateUrl())<a class="ui-link" href="{{ $certificate->certificateUrl() }}" target="_blank">Download file asli</a>@else Belum diunggah @endif</p></div></div></div></x-layouts::app>
